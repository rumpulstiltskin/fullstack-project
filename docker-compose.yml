services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/kanban.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
